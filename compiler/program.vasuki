def fib(n){
    if (n==0 or n==1) {
        return n;
    } else {
        return fib(n-1) + fib(n-2);
    };
};

print("Enter a number to find its Fibonacci number:");
var num = read_int();
print(fib(num));

